export default {
  // Which files ESLint should target
  files: ['**/*.js'],

  // ESLint rules
  rules: {
    semi: 'error',           // Require semicolons
    'no-unused-vars': 'warn' // Warn for unused variables
  }
};
